name: Deploy trigger
description: Deploy retraining trigger

inputs:
  - {name: image, type: String}

implementation:
  container:
    image: 127.0.0.1:5001/training:latest
    command: [
      python3,
      -m,
      pipeline.deploy_trigger,
      --image, {inputValue: image}
    ]
